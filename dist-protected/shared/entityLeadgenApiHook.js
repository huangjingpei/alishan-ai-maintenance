const {
  resolveEntityApiHooks,
  toJsRegexLiteral
} = require("./douyinRuntimeApiHooks");
function getEntityLeadgenApiHookInstaller(arg1) {
  const result = resolveEntityApiHooks(arg1);
  const result2 = toJsRegexLiteral(result.interesting);
  const result3 = toJsRegexLiteral(result.userish);
  const result4 = toJsRegexLiteral(result.relation);
  const result5 = toJsRegexLiteral(result.live);
  const result6 = toJsRegexLiteral(result.userSearch);
  return "(() => {\n    if (window.__radar_entity_api_hooked) return true;\n    window.__radar_entity_api_hooked = true;\n\n    const INTERESTING = " + result2 + ";\n    const USERISH = " + result3 + ";\n    const RELATION = " + result4 + ";\n    const LIVE_API = " + result5 + ";\n    const USER_SEARCH_API = " + result6 + ";\n\n    /** 从 sessionStorage / 页面 URL 读取用户搜索筛选，并写入 API 请求 */\n    const readUserSearchFilters = () => {\n      try {\n        const raw = sessionStorage.getItem('__radar_entity_user_search_filters');\n        if (raw) {\n          const parsed = JSON.parse(raw);\n          if (parsed && (parsed.douyin_user_fans || parsed.douyin_user_type)) return parsed;\n        }\n      } catch (_) { /* ignore */ }\n      try {\n        const page = new URL(window.location.href);\n        const fans = page.searchParams.get('douyin_user_fans') || '';\n        const type = page.searchParams.get('douyin_user_type') || '';\n        let selected = null;\n        const rawSelected = page.searchParams.get('filter_selected');\n        if (rawSelected) {\n          try { selected = JSON.parse(rawSelected); } catch (_) { selected = null; }\n        }\n        const out = {\n          douyin_user_fans: fans || (selected && selected.douyin_user_fans) || '',\n          douyin_user_type: type || (selected && selected.douyin_user_type) || '',\n        };\n        if (out.douyin_user_fans || out.douyin_user_type) return out;\n      } catch (_) { /* ignore */ }\n      return null;\n    };\n\n    const applyUserSearchFiltersToUrl = (inputUrl) => {\n      const url = String(inputUrl || '');\n      if (!url || !USER_SEARCH_API.test(url)) return url;\n      const filters = readUserSearchFilters();\n      if (!filters) return url;\n      try {\n        const u = new URL(url, window.location.origin);\n        const selected = {};\n        if (filters.douyin_user_fans) selected.douyin_user_fans = filters.douyin_user_fans;\n        if (filters.douyin_user_type) selected.douyin_user_type = filters.douyin_user_type;\n        if (!Object.keys(selected).length) return url;\n        u.searchParams.set('is_filter_search', '1');\n        u.searchParams.set('filter_selected', JSON.stringify(selected));\n        if (selected.douyin_user_fans) u.searchParams.set('douyin_user_fans', selected.douyin_user_fans);\n        if (selected.douyin_user_type) u.searchParams.set('douyin_user_type', selected.douyin_user_type);\n        return u.toString();\n      } catch (_) {\n        return url;\n      }\n    };\n\n    const cleanNick = (v) => String(v || '').trim().replace(/^@+/, '');\n    const readFollowerCount = (author) => {\n      if (!author || typeof author !== 'object') return null;\n      const raw = author.follower_count\n        ?? author.followerCount\n        ?? author.mplatform_followers_count\n        ?? author.mplatformFollowersCount\n        ?? author.fans_count\n        ?? author.fansCount;\n      const n = Number(raw);\n      return Number.isFinite(n) && n >= 0 ? n : null;\n    };\n    const formatCommentAgeFromTs = (raw) => {\n      const n = Number(raw);\n      if (!Number.isFinite(n) || n <= 0) return '';\n      const ms = n > 1e12 ? n : n * 1000;\n      const diffMs = Date.now() - ms;\n      if (!Number.isFinite(diffMs) || diffMs < 0) return '';\n      const mins = Math.floor(diffMs / 60000);\n      if (mins < 1) return '刚刚';\n      if (mins < 60) return mins + '分钟前';\n      const hours = Math.floor(mins / 60);\n      if (hours < 24) return hours + '小时前';\n      const days = Math.floor(hours / 24);\n      if (days < 30) return days + '天前';\n      const d = new Date(ms);\n      const y = d.getFullYear();\n      const m = d.getMonth() + 1;\n      const day = d.getDate();\n      if (y === new Date().getFullYear()) return m + '-' + day;\n      return y + '-' + m + '-' + day;\n    };\n    const readCommentMeta = (value) => {\n      if (!value || typeof value !== 'object') return { timeText: '', ipLocation: '' };\n      const timeText = formatCommentAgeFromTs(\n        value.create_time\n        ?? value.createTime\n        ?? value.comment_time\n        ?? value.commentTime\n        ?? value.publish_time\n        ?? value.publishTime\n        ?? (value.common && (value.common.create_time || value.common.createTime)),\n      );\n      let ipLocation = String(\n        value.ip_label\n        || value.ipLabel\n        || value.ip_label_text\n        || value.ipLabelText\n        || value.ip_label_v2\n        || value.ipLabelV2\n        || value.position\n        || '',\n      ).trim();\n      if (!ipLocation && Array.isArray(value.label_list)) {\n        const hit = value.label_list.find((item) => {\n          const t = String((item && (item.text || item.label || item.content)) || '').trim();\n          return t && !/赞|热评|作者|置顶/.test(t) && /^[\\u4e00-\\u9fffA-Za-z]{2,12}$/.test(t);\n        });\n        ipLocation = String((hit && (hit.text || hit.label || hit.content)) || '').trim();\n      }\n      if (!ipLocation && Array.isArray(value.comment_labels)) {\n        const hit = value.comment_labels.find((item) => {\n          const t = String((item && (item.text || item.label)) || '').trim();\n          const typ = String((item && (item.type || item.label_type)) || '');\n          return /属地|IP/i.test(typ) || (/^[\\u4e00-\\u9fff]{2,10}$/.test(t) && !/赞|热评|作者/.test(t));\n        });\n        ipLocation = String((hit && (hit.text || hit.label)) || '').replace(/^IP属地[:：\\s]*/i, '').trim();\n      }\n      return { timeText, ipLocation };\n    };\n    const isLikeCountOnlyText = (raw) => /^\\d+(?:\\.\\d+)?[万wWkK]?$/.test(String(raw || '').replace(/\\s+/g, ''));\n    const hasCommentPicture = (value) => {\n      if (!value || typeof value !== 'object') return false;\n      const pics = value.image_list || value.imageList || value.comment_image || value.commentImage;\n      if (Array.isArray(pics) && pics.length > 0) return true;\n      if (pics && typeof pics === 'object') return true;\n      return Number(value.image_list_length || value.imageListLength) > 0;\n    };\n    const readCommentText = (value) => {\n      if (!value || typeof value !== 'object') return '';\n      let text = String(value.text || value.content || '').trim();\n      if (text && hasCommentPicture(value) && isLikeCountOnlyText(text)) text = '';\n      if (text === '[图片评论]' || text === '[图片]') return '[图片]';\n      if (text && !isLikeCountOnlyText(text)) return text;\n      if (hasCommentPicture(value)) return '[图片]';\n      const sticker = value.sticker || value.emoji || value.sticker_info || value.stickerInfo;\n      if ((Array.isArray(sticker) && sticker.length) || (sticker && typeof sticker === 'object')\n        || value.sticker_id || value.stickerId) {\n        return '[表情或表情包]';\n      }\n      return text;\n    };\n    const pushAuthor = (bucket, author, hint, explicitContent = '', meta = {}) => {\n      if (!author || typeof author !== 'object') return;\n      const secUid = String(author.sec_uid || author.secUid || author.sec_id || '').trim();\n      const nickname = cleanNick(author.nickname || author.nickName || author.nick_name);\n      if (!secUid || secUid.length < 8 || !nickname) return;\n      if (/^(self|login)$/i.test(secUid)) return;\n      bucket.push({\n        secUid,\n        nickname,\n        userUrl: 'https://www.douyin.com/user/' + secUid,\n        followStatus: author.follow_status ?? author.followStatus ?? author.follower_status,\n        followerCount: readFollowerCount(author),\n        hint,\n        content: explicitContent || author.content || '',\n        timeText: String(meta.timeText || '').trim(),\n        time: String(meta.timeText || '').trim(),\n        ipLocation: String(meta.ipLocation || '').trim(),\n        location: String(meta.ipLocation || '').trim(),\n      });\n    };\n\n    const isNestedReplyComment = (value) => {\n      if (!value || typeof value !== 'object') return false;\n      const nonZero = (raw) => {\n        const text = String(raw == null ? '' : raw).trim();\n        return !!text && text !== '0';\n      };\n      if (nonZero(value.reply_id || value.replyId)) return true;\n      if (nonZero(value.reply_to_reply_id || value.replyToReplyId)) return true;\n      if (nonZero(value.reply_to_userid || value.reply_to_user_id || value.replyToUserId)) return true;\n      const level = Number(value.level || value.comment_level || value.commentLevel);\n      return Number.isFinite(level) && level >= 2;\n    };\n    const COMMENT_API = /\\/comment\\/list(?!\\/reply)/i;\n\n    const guessHint = (url) => {\n      const u = String(url || '');\n      if (LIVE_API.test(u) || u.includes('/webcast/')) return 'live';\n      if (RELATION.test(u)) return 'mutual';\n      if (USERISH.test(u)) return 'user';\n      if (COMMENT_API.test(u)) return 'comment';\n      if (INTERESTING.test(u)) return 'blogger';\n      return 'any';\n    };\n\n    const walk = (value, hint, bucket, depth = 0, seen) => {\n      if (!value || depth > 10) return;\n      if (typeof value !== 'object') return;\n      if (seen.has(value)) return;\n      seen.add(value);\n      if (Array.isArray(value)) {\n        value.forEach((item) => walk(item, hint, bucket, depth + 1, seen));\n        return;\n      }\n\n      let liveEventContent = '';\n      if (hint === 'live') {\n        const method = String(value.method || value.common?.method || value.event_type || '');\n        if (method.includes('ChatMessage') || value.content) {\n          const txt = String(value.content || value.text || '').trim();\n          liveEventContent = txt ? '发表弹幕: ' + txt : '直播间发言';\n        } else if (method.includes('MemberMessage')) {\n          liveEventContent = '进入直播间';\n        } else if (method.includes('LikeMessage')) {\n          liveEventContent = '点赞了直播间';\n        } else if (method.includes('GiftMessage')) {\n          const giftName = value.gift?.name || value.gift_name || value.giftName || '';\n          liveEventContent = giftName ? '赠送礼物: ' + giftName : '赠送礼物';\n        } else if (method.includes('SocialMessage')) {\n          liveEventContent = '关注了主播';\n        } else if (method.includes('UserSeqMessage') || method.includes('RankMessage')) {\n          liveEventContent = '直播间在线观众';\n        }\n      }\n\n      const aweme = value.aweme_info || value.awemeInfo || value.aweme || value.aweme_detail;\n      if (aweme && typeof aweme === 'object') {\n        pushAuthor(\n          bucket,\n          aweme.author || aweme.authorInfo || aweme.author_info,\n          hint === 'user' ? 'blogger' : hint,\n          liveEventContent,\n        );\n      }\n      const skipNestedReply = hint === 'comment' && isNestedReplyComment(value);\n      const userInfo = value.user_info || value.userInfo || value.user;\n      if (!skipNestedReply && userInfo && typeof userInfo === 'object') {\n        const commentText = hint === 'comment'\n          ? readCommentText(value)\n          : liveEventContent;\n        const meta = hint === 'comment' ? readCommentMeta(value) : {};\n        pushAuthor(bucket, userInfo, hint, commentText, meta);\n      }\n      if (!skipNestedReply\n        && (value.sec_uid || value.secUid)\n        && (value.nickname || value.nickName || value.nick_name)) {\n        const commentContent = hint === 'comment'\n          ? readCommentText(value)\n          : (value.text || value.content || liveEventContent || '');\n        const meta = hint === 'comment' ? readCommentMeta(value) : {};\n        pushAuthor(bucket, value, hint, commentContent, meta);\n      }\n      [\n        'user_list', 'userList', 'users', 'followers', 'followings',\n        'follow_list', 'data', 'cards', 'aweme_list', 'business_data', 'messages',\n        'comments', 'comment', 'reply_comments', 'reply_comment',\n      ].forEach((key) => {\n        if (value[key] == null) return;\n        // 评论潜客只采一级：不进入 reply_comments / reply_comment\n        if (hint === 'comment' && /^reply_comments?$/i.test(key)) return;\n        walk(value[key], hint, bucket, depth + 1, seen);\n      });\n    };\n\n    const report = (url, data) => {\n      try {\n        if (!data || typeof data !== 'object') return;\n        const hint = guessHint(url);\n        if (hint === 'any' && !INTERESTING.test(String(url || ''))) return;\n        const bucket = [];\n        walk(data, hint, bucket, 0, new WeakSet());\n        if (!bucket.length) return;\n        const dedup = new Map();\n        bucket.forEach((item) => {\n          const prev = dedup.get(item.secUid);\n          if (!prev) {\n            dedup.set(item.secUid, item);\n            return;\n          }\n          dedup.set(item.secUid, {\n            ...prev,\n            ...item,\n            content: item.content || prev.content || '',\n            timeText: item.timeText || prev.timeText || '',\n            time: item.time || prev.time || item.timeText || prev.timeText || '',\n            ipLocation: item.ipLocation || prev.ipLocation || '',\n            location: item.location || prev.location || item.ipLocation || prev.ipLocation || '',\n          });\n        });\n        document.dispatchEvent(new CustomEvent('__radar_entity_api', {\n          detail: {\n            url: String(url || '').slice(0, 240),\n            hint,\n            users: Array.from(dedup.values()),\n          },\n        }));\n      } catch (_) { /* ignore */ }\n    };\n\n    const parseBody = (url, body) => {\n      if (!body) return;\n      try {\n        if (typeof body === 'string') {\n          const text = body.trim();\n          if (!text || (text[0] !== '{' && text[0] !== '[')) return;\n          report(url, JSON.parse(text));\n          return;\n        }\n        if (typeof body === 'object') report(url, body);\n      } catch (_) { /* ignore */ }\n    };\n\n    const originalFetch = window.fetch;\n    if (typeof originalFetch === 'function') {\n      window.fetch = async function (...args) {\n        try {\n          if (typeof args[0] === 'string') {\n            args[0] = applyUserSearchFiltersToUrl(args[0]);\n          } else if (args[0] && typeof args[0] === 'object' && args[0].url) {\n            const nextUrl = applyUserSearchFiltersToUrl(args[0].url);\n            if (nextUrl !== args[0].url && typeof Request === 'function') {\n              args[0] = new Request(nextUrl, args[0]);\n            }\n          }\n        } catch (_) { /* ignore */ }\n        const response = await originalFetch.apply(this, args);\n        try {\n          const req = args[0];\n          const url = typeof req === 'string' ? req : (req && req.url) || '';\n          if (INTERESTING.test(url) || USERISH.test(url) || RELATION.test(url)) {\n            const clone = response.clone();\n            clone.text().then((text) => parseBody(url, text)).catch(() => {});\n          }\n        } catch (_) { /* ignore */ }\n        return response;\n      };\n    }\n\n    const XHR = window.XMLHttpRequest;\n    if (XHR && XHR.prototype) {\n      const open = XHR.prototype.open;\n      const send = XHR.prototype.send;\n      XHR.prototype.open = function (method, url, ...rest) {\n        const nextUrl = applyUserSearchFiltersToUrl(url);\n        this.__radar_entity_url = nextUrl;\n        return open.call(this, method, nextUrl, ...rest);\n      };\n      XHR.prototype.send = function (...args) {\n        this.addEventListener('load', function () {\n          try {\n            const url = String(this.__radar_entity_url || '');\n            if (!(INTERESTING.test(url) || USERISH.test(url) || RELATION.test(url))) return;\n            parseBody(url, this.responseText);\n          } catch (_) { /* ignore */ }\n        });\n        return send.apply(this, args);\n      };\n    }\n    return true;\n  })();";
}
module.exports = {
  getEntityLeadgenApiHookInstaller: getEntityLeadgenApiHookInstaller
};